import {combineReducers} from 'redux'

const user = (state = {}, action) => {
  switch(action.type) {
  default:
    return state
  }
}

const something = (state = {}, action) => {
  switch(action.type) {
  default:
    return state
  }
}

export {
  user,
  something
}